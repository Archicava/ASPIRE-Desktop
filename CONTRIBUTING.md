# Contributing

## Development

1. Install dependencies: `npm ci`
2. Start desktop app in dev mode: `npm run dev`
3. Validate before opening a PR:
   - `npm run lint`
   - `npm run build`

## Pull Requests

1. Keep PRs focused and small.
2. Describe behavior changes and test steps.
3. Include screenshots for UI changes.
